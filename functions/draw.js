// Declare a function draw
function draw() {
  console.log("Teams proceed to penalties to determine the winner of the game");
}

// call the function draw
draw();

/*
Function declaration: function draw(){...} defines a function draw. Inside the curly braces you put the code you want to run whenever the code is called.
Calling the function: draw(); calls the function, meaning JavaScript goes to the draw function and executes the code inside it */
